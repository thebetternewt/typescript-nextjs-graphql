import { DocumentLoader, DocumentFile } from './document-loader';
import { DocumentNode } from 'graphql';
import { ExtractOptions } from '../../utils/extract-document-string-from-code-file';
export interface DocumentsFromGlobOptions extends ExtractOptions {
    ignore?: string | ReadonlyArray<string>;
}
export declare class DocumentsFromGlob implements DocumentLoader {
    canHandle(doc: string): Promise<boolean> | boolean;
    documentsFromGlobs(documentGlob: string, options?: DocumentsFromGlobOptions): Promise<string[]>;
    loadFileContent(filePath: string, options?: DocumentsFromGlobOptions): Promise<DocumentNode | null>;
    loadDocumentsSources(filePaths: string[], options?: DocumentsFromGlobOptions): Promise<DocumentFile[]>;
    handle(doc: string, options?: DocumentsFromGlobOptions): Promise<DocumentFile[]>;
}
