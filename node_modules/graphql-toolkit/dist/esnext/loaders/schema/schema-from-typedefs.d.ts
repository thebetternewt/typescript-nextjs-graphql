import { SchemaLoader } from './schema-loader';
import { DocumentNode } from 'graphql';
import { ExtractOptions } from '../../utils';
export declare function isGraphQLFile(globPath: string): boolean;
export declare class SchemaFromTypedefs implements SchemaLoader {
    canHandle(globOrValidPath: string): boolean;
    handle(globPath: string, options?: ExtractOptions): Promise<DocumentNode>;
}
